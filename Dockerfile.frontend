# CVD Predictor Frontend Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy frontend files
COPY frontend/ .

# Install simple HTTP server
RUN npm install -g http-server

# Expose port
EXPOSE 8080

# Run server
CMD ["http-server", "-p", "8080", "--gzip"]
